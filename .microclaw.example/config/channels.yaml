# 功能开关：启用的 Provider 和 Channel
# 支持的 providers: openai, claude
# 支持的 channels: feishu, wecom, wechat

features:
  providers:
    - openai
    - claude
  channels:
    - feishu
    - wecom
    - wechat
